@{
    var version = typeof(IdentityServer4.Hosting.IdentityServerMiddleware).Assembly.GetName().Version.ToString();
}

<h1 class="mt-5">Portal Login ekranına hoş geldiniz.</h1>
<p class="lead">@@alidogankim <small>(version @version)</small></p>